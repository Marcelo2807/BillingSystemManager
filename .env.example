# Database Configuration
# Para Replit: O Replit provisiona automaticamente via Neon
# Para local: Use sua URL do PostgreSQL
DATABASE_URL=postgresql://user:password@host:5432/database

# OpenAI API (Opcional - para processamento de documentos com IA)
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Server Configuration
PORT=5000
NODE_ENV=development

# Notas:
# 1. Renomeie este arquivo para .env
# 2. Preencha com seus valores reais
# 3. NUNCA faça commit do arquivo .env (já está no .gitignore)
# 4. No Replit, use a aba "Secrets" para configurar as variáveis
